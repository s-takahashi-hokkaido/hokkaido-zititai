# 小樽市 フォーム調査結果

- URL: https://www.city.otaru.lg.jp/contact/
- 検出要素数: input/textarea/select = 2, button = 0

## 推奨フィールドテーブル（MDへコピペ）

> 「フィールド名」列は自動推定。`?` は要手動マッピング。

| フィールド名 | セレクタ | 入力タイプ | 必須 | 固定値 |
| --- | --- | --- | --- | --- |
| ? | `#gsc-i-id1` | text |  |  |
| ? | `#gsc-i-id2` | text |  |  |

## 送信ボタン候補

（検出なし — ページに submit ボタンが見えていない可能性）

## 生データ

1. **tag**=input **type**='text' **name**='search' **id**='gsc-i-id1' **required**=False
    - aria-label: 検索
2. **tag**=input **type**='text' **name**='search' **id**='gsc-i-id2' **required**=False
    - aria-label: 検索
    - rowContext: ×
