# 釧路市 フォーム調査結果

- URL: https://www.city.kushiro.lg.jp/cgi-bin/contacts/G0203010
- 検出要素数: input/textarea/select = 1, button = 1

## 推奨フィールドテーブル（MDへコピペ）

> 「フィールド名」列は自動推定。`?` は要手動マッピング。

| フィールド名 | セレクタ | 入力タイプ | 必須 | 固定値 |
| --- | --- | --- | --- | --- |
| ? | `#key` | text |  |  |

## 送信ボタン候補

| セレクタ | 種別 | ラベル/name |
| --- | --- | --- |
| `[name="submit"]` | button | 目的別で探す / submit |

## 生データ

1. **tag**=input **type**='hidden' **name**='cx' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
2. **tag**=input **type**='hidden' **name**='ie' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
3. **tag**=input **type**='text' **name**='q' **id**='key' **required**=False
    - ancestorHeading: 目的別で探す
4. **tag**=input **type**='image' **name**='submit' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
5. **tag**=input **type**='hidden' **name**='siteurl' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
6. **tag**=input **type**='hidden' **name**='ref' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
7. **tag**=input **type**='hidden' **name**='ss' **id**='' **required**=False
    - ancestorHeading: 目的別で探す
